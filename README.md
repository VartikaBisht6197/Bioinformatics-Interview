# Bioinformatics Interview – Bash Skills

## Q1. Directory Structure
Create the following directory structure:
```
project/
├── data/
├── results/
└── scripts/
```

---

## Q2. Create a File
Create a file called `samples.txt` that contains:
```
sampleA
sampleB
sampleC
```

---

## Q3. Count Lines
Given the file `samples.txt`, print how many lines it contains.

---

## Q4. List Files
In the folder `data`, list only the `.tsv` files.

---

## Q5. Search for a Gene
In the file `file1.txt` (within the `data` folder), check whether `geneD` is present.

---

## Q6. Count Lines in Another File
How many lines does `file2.txt` (within the `data` folder) have?

---

## Q7. Print First Lines
Print only the first 2 lines from `file3.txt` (within the `data` folder).

---

## Q8. Print a Column
Print out only the 2nd column from `file3.txt` (within the `data` folder).

---

## Q9.  Sort a File
Sort `file2.txt` (within the `data` folder) by the 2nd column.